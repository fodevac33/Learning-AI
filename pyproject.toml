[project]
name = "nova"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.0",
    "tinygrad>=0.10.2",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
